const findYarnWorkspaceRoot = (path: string): string => {
	return ''
}

const findYarnWorkspaceRootSync = (path: string): string => {
	return ''
}

export { findYarnWorkspaceRootSync, findYarnWorkspaceRoot, findYarnWorkspaceRoot as default }
